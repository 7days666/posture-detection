{
  "name": "posture-detection-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create posture-detection-db",
    "db:migrate": "wrangler d1 execute posture-detection-db --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute posture-detection-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "wrangler": "^3.22.0"
  },
  "dependencies": {
    "hono": "^3.12.0"
  }
}
